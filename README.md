# Peresin-BackEnd
Portfolio de mis habilidades Back-End

## Build

Levantar de manera local

´´´
$env:DB_URL="jdbc:mysql://localhost:3306/backenpti?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC"
$env:DB_USER="root"
$env:DB_PASSWORD="root"

.\mvnw.cmd spring-boot:run
´´´

Run `./mvnw spring-boot:run` to build the project.